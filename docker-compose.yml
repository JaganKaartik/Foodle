version: '3'
services:
  app:
    container_name: foodle-api-server
    restart: always
    build: .
    ports:
      - '7000:7000'
    external_links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'
