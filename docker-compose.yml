version: '3'
services:
  app:
    container_name: foodle-api-server
    restart: always
    build: .
    env_file: test.env
    ports:
      - '7000:7000'
    depends_on:
      - mongo
    networks:
      - foodle-app
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db
    networks:
      - foodle-app
volumes:
  mongo-data:
    name: ${VOLUME_ID}

networks:
  foodle-app:
    driver: bridge
